name: obfuscate
description: A Flutter project to test the obfucation issue.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=2.16.1 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  firebase_messaging: ^11.2.6

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true